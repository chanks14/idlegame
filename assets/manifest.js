'use strict';
// Optional asset overrides. Map an icon name to a file in /assets to replace the built-in SVG, e.g.
//   food: 'assets/icons/food.png',
//   era_stone: 'assets/icons/stone-age.svg',
// Icon names are listed in js/ui/icons.js (resources, generators, agents, planets, factions, ships…).
(function () {
  const IG = globalThis.IG || (globalThis.IG = {});
  IG.ASSET_OVERRIDES = {
  };
})();
